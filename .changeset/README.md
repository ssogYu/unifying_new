# Changesets

Add your changelog entries here when making a release.

To add a changeset:

```bash
pnpm changeset
```

This will prompt you to select which packages changed and whether it's a major, minor, or patch change.
